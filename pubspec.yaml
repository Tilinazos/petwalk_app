name: petwalk_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0        
  dartz: ^0.10.1
  get_it: ^8.2.0
  provider: ^6.0.5
  equatable: ^2.0.7
  flutter_map: ^6.1.0 # Versión reciente
  latlong2: ^0.9.1  # Para manejar LatLng (necesario por flutter_map)
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
